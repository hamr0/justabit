# ActionClass classifier — moved

**Moved 2026-09-06** to the `rwxmap` repo, `docs/product/prd.md`
(`/home/hamr/PycharmProjects/rwxmap`, GitHub remote pending). The outline
this file held is commit `fcca38f` in this repo's history.

The tool is not load-bearing for the CAMARA or IETF submissions. It is a
supporting proof-of-concept and the author's own tool. Nothing in
`docs/product/prd.md`, `camara/`, or `ietf/` depends on it.
